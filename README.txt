Makefile is constructed by AI.
To use Makefile:
    1. run "make" in terminal
        - This will create "chash" file
    2. run "./chash" in terminal to run file.

    Any .c files you add to folder, add to Makefile line 9